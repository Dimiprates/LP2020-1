/*#include <stdio.h>

int main(void){
  float total,final;
	printf("Digite a soma ");
	scanf("%f",&total);
	if((total>100) && (total<200)) {
		final = total - ( total*0.05);
		printf("R$ %.2f",final);
	}else if (total>200){
		final = total - ( total*0.1);
		printf("R$ %.2f",final);
		}else{
			printf("R$ %.2f",total);	
		}
  return 0;
	} */